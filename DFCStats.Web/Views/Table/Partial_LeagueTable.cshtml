@model List<DFCStats.Web.Models.Tables.Tables>

<table class="table">
	<thead>
		<tr>
			<th colspan="3" class="text-center table-light border-end"></th>
			<th colspan="5" class="text-center table-light border-end border-top">Home</th>
			<th colspan="5" class="text-center table-light border-end border-top">Away</th>
			<th colspan="7" class="text-center table-light"></th>
		</tr>
		<tr>
			<th class="table-light text-center">Pos</th>
			<th class="table-light">Team</th>
			<th class="table-light text-center border-end">Ply</th>
			<th class="table-light text-center">W</th>
			<th class="table-light text-center">D</th>
			<th class="table-light text-center">L</th>
			<th class="table-light text-center">GF</th>
			<th class="table-light text-center border-end">GA</th>
			<th class="table-light text-center">W</th>
			<th class="table-light text-center">D</th>
			<th class="table-light text-center">L</th>
			<th class="table-light text-center">GF</th>
			<th class="table-light text-center border-end">GA</th>
			<th class="table-light text-center">GD</th>
			<th class="table-light text-center">Pts</th>
			<th class="table-light">Notes</th>
		</tr>
	</thead>
	<tbody class="table-group-divider">
        @if (Model != null)
		{
			if (Model.Count == 0)
			{
				<tr>
					<td colspan="16" class="text-center table-warning text-warning-emphasis">There is no league table for this season yet</td>
				</tr>
			} else {

				foreach (var t in Model)
				{
					// Sets the rowClass based on the champion, promotion, play-off and relegation flags
					string rowClass = t.IsChampion || t.IsPromotion ? "table-success" : t.IsPlayOff ? "table-warning" : t.IsRelegation ? "table-danger" : "table-light";

					<tr>
						<td class="@rowClass text-center">@t.Position</td>
						<td class="@rowClass">@t.ClubName</td>
						<td class="@rowClass text-center border-end">@t.GamesPlayed</td>
						<td class="@rowClass text-center">@t.HomeGamesWon</td>
						<td class="@rowClass text-center">@t.HomeGamesDrawn</td>
						<td class="@rowClass text-center">@t.HomeGamesLost</td>
						<td class="@rowClass text-center">@t.HomeGoalsFor</td>
						<td class="@rowClass text-center border-end">@t.HomeGoalsAgainst</td>
						<td class="@rowClass text-center">@t.AwayGamesWon</td>
						<td class="@rowClass text-center">@t.AwayGamesDrawn</td>
						<td class="@rowClass text-center">@t.AwayGamesLost</td>
						<td class="@rowClass text-center">@t.AwayGoalsFor</td>
						<td class="@rowClass text-center border-end">@t.AwayGoalsAgainst</td>
						<td class="@rowClass text-center">@t.GoalDifference</td>
						<td class="@rowClass text-center">@t.Points</td>
						<td class="@rowClass">@t.Notes</td>
						<td class="@rowClass text-end">
							@if (t.Id != Model.First().Id)
							{
								<button class="btn btn-primary" onclick="moveClub(@string.Format("'{0}'", t.Id), 'up')"><i class="fa-solid fa-arrow-up"></i></button>
							}
						</td>
						<td class="@rowClass text-start">
							@if (t.Id != Model.Last().Id)
							{
								<button class="btn btn-primary" onclick="moveClub(@string.Format("'{0}'", t.Id), 'down')"><i class="fa-solid fa-arrow-down"></i></button>
							}
						</td>
						<td class="@rowClass"><button class="btn btn-primary" onclick="editClub(@string.Format("'{0}'", t.Id))">Edit</button></td>
						<td class="@rowClass"><button class="btn btn-danger" onclick="removeClub(@string.Format("'{0}'", t.Id))">Remove</button></td>
					</tr>
				}
			}
		} else {
			<tr>
				<td colspan="16" class="text-center table-danger text-danger-emphasis">There was a problem getting the league table for this season - please refresh the page</td>
			</tr>
		}
    </tbody>
</table>